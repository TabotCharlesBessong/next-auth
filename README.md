# next-auth
Building a universally reusable authentication system or authentication blue print for next js application. This will help save time when building a next js application as all authentication will be handled already also considering all role base access
